services:
  vulnerable-login:
    build: .
    container_name: vulnerable-login-lab
    ports:
      - "5000:5000"
    volumes:
      - ./logs:/app/logs
    environment:
      - FLASK_RUN_HOST=0.0.0.0
      - FLASK_RUN_PORT=5000
      - DEFENSE_ENABLED=0
      - ADAPTIVE_BLOCK_SCORE=80
